# Task 02 Answer

1. 当使用`./tools/recruit-info` 时，Shell能够明确的知道程序的路径，于是可以直接执行该文件，但若直接输入'recruit-info'时，Shell只会在PATH环境变量列出目录寻找名为此的可执行文件，当前目录通常不在PATH中，所以找不到，于是报错
2. Shell会按PATH环境变量目录中顺序依次查找名为此的可执行文件，找到第一个时则执行

